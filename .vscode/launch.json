{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Vite: dev + debug browser",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run dev",
            "serverReadyAction": {
                "pattern": ".*(http://localhost:[0-9]+).*",
                "uriFormat": "%s",
                "action": "openIntegratedBrowser"
            }
        }
    ]
}